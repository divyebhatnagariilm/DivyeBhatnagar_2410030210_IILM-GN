-- (a)
SELECT * FROM EMPLOYEE
WHERE HIREDATE < '30-JUN-80'
OR HIREDATE > '31-DEC-81';

-- (b)
SELECT ENAME FROM EMPLOYEE WHERE ENAME LIKE 'A%';

-- (c)
SELECT ENAME FROM EMPLOYEE WHERE LENGTH(ENAME)=5;

-- (d)
SELECT ENAME FROM EMPLOYEE WHERE ENAME LIKE 'A%';

-- (e)
SELECT ENAME FROM EMPLOYEE
WHERE JOB NOT IN ('SALESMAN','CLERK','ANALYST');

-- (f)
SELECT ENAME, SAL12 AS ANNUAL_SAL
FROM EMPLOYEE
ORDER BY ANNUAL_SAL DESC;

-- (g)
SELECT ENAME, SAL, (SAL12) AS ANNUAL,
(SAL0.15) AS DA,
(SAL0.10) AS HRA,
(SAL0.05) AS PF,
(SAL12 + SAL0.15 + SAL0.10 - SAL0.05) AS TOTAL
FROM EMPLOYEE;

-- (h)
UPDATE EMPLOYEE
SET SAL = SAL * 1.10
WHERE COMM IS NULL;

-- (i)
SELECT * FROM EMPLOYEE
WHERE SAL1.20 > 3000;

-- (j)
SELECT * FROM EMPLOYEE
WHERE SAL LIKE '_%';